## How to Run

### Prerequisites

To run the program, make sure that you first have the necessary JDK for the job.

---

### Clone the Repository

Secondly, you must clone the code from the repository by following these steps.

1. Open Command Prompt by either going to search for it on the taskbar or by pressing **Ctrl + Shift + P**.
2. Change your directory on where you would like the files to be located at by putting:

```bash
cd PATH/OF/YOUR/FILE
```

3. Clone the code from the repository by typing:

```bash
git clone https://github.com/dihhcoder/MC02GRP9.git
```

---

### Compile and Run

Next step is to run the program by compiling the code and then running it.

#### Step 1

Verify if you have a JDK installed on your device by typing:

```bash
javac -version
```

If it returns a version of `javac` then you have one installed. Else, you must install one from the internet.

I would recommend downloading one from Oracle.

https://www.oracle.com/java/technologies/downloads/

After installation, verify its existence by typing:

```bash
javac -version
```

#### Step 2

Type:

```bash
javac Main.java
```

on your command prompt.

If the program compiles without error then it is safe to run.

#### Step 3

Run the program by typing:

```bash
java Main
```

#### Step 4

Enjoy Arrow Mail Simulator 2!